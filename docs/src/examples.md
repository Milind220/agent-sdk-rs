# Examples

## Local Scripted Loop

Source of truth from repository:

```rust
{{#include ../../examples/local_loop.rs}}
```

Run:

```bash
cargo run --example local_loop
```

## Dependency Override

Source of truth from repository:

```rust
{{#include ../../examples/di_override.rs}}
```

Run:

```bash
cargo run --example di_override
```
